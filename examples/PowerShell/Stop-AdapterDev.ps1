# An example that shows how you can use Azure CLI 2.0 within PowerShell to tear down a dev environment easily
# Remember to set execution policy:
# > Set-ExecutionPolicy -Scope CurrentUser RemoteSigned

az group delete -n ExampleGroup